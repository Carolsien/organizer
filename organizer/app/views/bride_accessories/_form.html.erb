<%= form_for(bride_accessory) do |f| %>
  <% if bride_accessory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bride_accessory.errors.count, "error") %> prohibited this bride_accessory from being saved:</h2>

      <ul>
      <% bride_accessory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :color %>
    <%= f.text_field :color %>
  </div>

  <div class="field">
    <%= f.label :size %>
    <%= f.date_select :size %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
