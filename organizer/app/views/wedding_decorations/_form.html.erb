<%= form_for(wedding_decoration) do |f| %>
  <% if wedding_decoration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wedding_decoration.errors.count, "error") %> prohibited this wedding_decoration from being saved:</h2>

      <ul>
      <% wedding_decoration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Nazwa' %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :colour, 'Kolor' %>
    <%= f.text_field :colour %>
  </div>

  <div class="field">
    <%= f.label :amount, 'Ilosc' %>
    <%= f.number_field :amount %>
  </div>

  <div class="actions">
    <%= f.submit 'Potwierdz' %>
  </div>
<% end %>
