<%= form_for(guest) do |f| %>
  <% if guest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guest.errors.count, "error") %>Nie można dodać elementu</h2>

      <ul>
      <% guest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Imie' %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :subname, 'Nazwisko' %>
    <%= f.text_field :subname %>
  </div>

  <div class="field">
    <%= f.label :description, 'Osoba Towarzyszaca' %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :invitation_sent, 'Wyslano zaproszenie' %>
    <%= f.check_box :invitation_sent %>
  </div>

  <div class="field">
    <%= f.label :invitation_confirmed, 'Zaproszenie potwierdzono' %>
    <%= f.check_box :invitation_confirmed %>
  </div>
  <div class="actions">
    <%= f.submit 'Potwierdź' %>
  </div>
<% end %>
