<p id="notice"><%= notice %></p>

<h1>Obraczki</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nazwa modelu</th>
      <th>Rozmiar</th>
      <th colspan="3">Opcje</th>
    </tr>
  </thead>

  <tbody>
    <% @church_rings.each do |church_ring| %>
      <tr>
        <td><%= church_ring.name %></td>
        <td><%= church_ring.size %></td>
        <td><%= link_to 'Pokaż', church_ring %></td>
        <td><%= link_to 'Edytuj', edit_church_ring_path(church_ring) %></td>
        <td><%= link_to 'Usuń', church_ring, method: :delete, data: { confirm: 'Jesteś pewny/a' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Dodaj', new_church_ring_path %> |
<%= link_to 'Wróć', church_path %>
